version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install -r requirements.txt.txt
  build:
    commands:
      - python a001.py

artifacts:
  files:
    - telecom_tower_model.pkl # this is yml file
